Place best.onnx here before building the Docker image.

Copy it from:
  runs/cats_v2_run3_stage2/weights/best.onnx

Command:
  copy runs\cats_v2_run3_stage2\weights\best.onnx container\models\best.onnx
